# TEST yml
spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  session:
    store-type: jdbc

# TEST OAuth
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: test
            secret: test
            scope: profile,email
